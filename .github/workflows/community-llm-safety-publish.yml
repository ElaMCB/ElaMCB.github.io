# Publishes the next article for community/llm-safety-red-team/ every intervalDays (default 21 in manifest).

name: Community LLM Safety publish

on:
  schedule:
    - cron: '0 15 * * *'
  workflow_dispatch:
    inputs:
      force:
        description: 'Publish next queued article now (ignore 21-day interval)'
        required: false
        default: false
        type: boolean

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Publish next article (if due)
        env:
          COMMUNITY_SERIES_SUBDIR: llm-safety-red-team
        run: |
          FORCE_PUBLISH=0
          if [ "${GITHUB_EVENT_NAME}" = "workflow_dispatch" ]; then
            INPUT_FORCE='${{ github.event.inputs.force }}'
            if [ "${INPUT_FORCE}" = "true" ] || [ "${INPUT_FORCE}" = "True" ]; then
              FORCE_PUBLISH=1
            fi
          fi
          export FORCE_PUBLISH
          echo "FORCE_PUBLISH=${FORCE_PUBLISH} (event=${GITHUB_EVENT_NAME})"
          python3 scripts/community_llm_safety_publish.py

      - name: Commit and push updates
        env:
          SERIES_DIR: community/llm-safety-red-team
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add "$SERIES_DIR/manifest.json" "$SERIES_DIR/topics-queue.json" "$SERIES_DIR/articles/"
          if git diff --staged --quiet; then
            echo "No publish this run (interval not reached, empty queue, or already up to date)."
            exit 0
          fi
          git commit -m "Community: publish LLM Safety & Red-Teaming article [skip ci]"
          git pull --rebase origin "${GITHUB_REF_NAME}"
          git push origin "HEAD:${GITHUB_REF_NAME}"
